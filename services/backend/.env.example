# Example env file for backend
# Use when running locally. In production, use secret manager.

# Base URL accessible by frontend (optional). If blank, frontend should use full host when calling backend.
API_BASE_URL=http://localhost:8000
